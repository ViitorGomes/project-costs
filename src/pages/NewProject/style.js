import styled from "styled-components";

export const NewProject = styled.main`

    display: flex;
    align-items: center;
    justify-content: center;

    h1 {
        color: ${props => props.theme.font.font1};
        font-size: 2rem;
    }

`